# Liar's Deck v2.0.3 更新日志

**发布时间**：2026-08-29

---

### 自更新与端口无缝交接
- **无依赖自更新**：引入纯 Go 原生解压与热替换机制，彻底摆脱主机需要安装 `go` 编译器或 `tar`/`unzip` 的外部依赖；
- **优雅端口交接**：新增 `--wait-pid` 启动参数，确保旧进程彻底退出释放端口后新进程再绑定端口，实现 0 端口冲突重启。

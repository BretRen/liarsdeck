# Liar's Deck v2.3.4 更新日志

**发布时间**：2026-08-31

---

### 断线时间池与 PausedPlayerID 严格绑定
- **断线保护升级为 30 秒累积时间池**：将原有一刀切的单次布尔断线标记升级为 `DisconnectGraceRemaining int` 时间池；玩家单局断线按实际掉线秒数扣除，剩余秒数保留在时间池中，彻底消除网络偶发瞬断导致的直接出局误杀；
- **暂停与超时全生命周期严格绑定 `PausedPlayerID`**：`GameState` 增加 `PausedPlayerID string`，暂停倒计时、重连恢复与超时处死全生命周期严格绑定 Zitadel `sub`，彻底与可变 `nickname` 解耦；
- **保持经典座次博弈**：保留每小轮发牌开局时的玩家席位随机洗牌（`CryptoShuffle`），确保每轮出牌与质疑顺位的博弈趣味。
